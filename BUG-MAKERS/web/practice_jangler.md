## Описание

Задание от команды DUCKERZ.
Представление окончено! Теперь вам самомим предстоит научиться еще одному трюку настоящего жонглера.

## Решение

Информация:
```
Сравнение флага через strcmp() — это типичная уязвимость типа PHP type juggling. 

Если передать массив вместо строки (flag[]=...), strcmp() вернёт NULL, а NULL == 0 будет true.
То есть можно обойти проверку, отправив flag[]=anything.

Передаем через POST запрос параметр flag[] = test, получаем флаг:
```
DUCKERZ{AnO7Her_taSK_On_php_TYp3_ju66l!N9}
